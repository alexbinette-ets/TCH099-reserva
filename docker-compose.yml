version: '3'

services:
  mongodb:
    container_name: mongodb_1
    build:
      context: mongo
    volumes:
      - mongodb-data:/data/db
    ports:
      - "27017:27017"
    networks:
      - express-mongo
    expose:
      - 27017

  express:
    container_name: express_1
    build:
      context: express
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
    networks:
      - express-mongo
    expose:
      - 5000

  mongo-express:
    container_name: mongo-express_1
    image: mongo-express
    ports:
      - "8081:8081"
    depends_on:
      - mongodb
    environment:
      - ME_CONFIG_MONGODB_URL=mongodb://mern-mongodb_1:27017
      - ME_CONFIG_MONGODB_SERVER=mern-mongodb_1
      - ME_CONFIG_MONGODB_PORT=27017
    networks:
      - express-mongo
volumes:
  mongodb-data:
networks:
  express-mongo: